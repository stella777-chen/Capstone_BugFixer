To run the build, please execute the following steps:


Step 1 - Install Node on you machine (Enabler to run npm commands)
Step 2 - Install the node modules using "npm i"
Step 3 - Now, make a build file - "npm run build"
Step 4 - Run the build to open the dashboard in machine's localhost - "npm run"
